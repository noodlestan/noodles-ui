import { EntityType } from './compiler';

export const NUI: Record<EntityType, EntityType> = {
    project: 'project',
    surface: 'surface',
    theme: 'theme',
    variant: 'variant',
    component: 'component',
    token: 'token',
    mixin: 'mixin',
};
