import { EntityType } from './projects';

export const NUI: Record<EntityType, EntityType> = {
    surface: 'surface',
    theme: 'theme',
    variant: 'variant',
    component: 'component',
    token: 'token',
    mixin: 'mixin',
};
