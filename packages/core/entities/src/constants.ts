import { EntityType } from './types';

export const NUI: Record<EntityType, EntityType> = {
    project: 'project',
    surface: 'surface',
    theme: 'theme',
    variant: 'variant',
    component: 'component',
    token: 'token',
    mixin: 'mixin',
};
