import { EntityType } from './types';

export const NUI: Record<EntityType, EntityType> = {
    system: 'system',
    surface: 'surface',
    theme: 'theme',
    variant: 'variant',
    component: 'component',
    token: 'token',
    mixin: 'mixin',
};
